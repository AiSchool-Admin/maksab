# ══════════════════════════════════════════════════════════
# مكسب — المتغيرات البيئية
# انسخ هذا الملف إلى .env.local واملأ القيم
# cp .env.local.example .env.local
# ══════════════════════════════════════════════════════════

# ─── Supabase (مطلوب) ────────────────────────────────────
# Get from: https://supabase.com/dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service role key — used by API routes (bypasses RLS)
# Get from: Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ─── المصادقة (مطلوب) ──────────────────────────────────
# مفتاح توقيع رموز OTP — أنشئه بالأمر:
# openssl rand -hex 32
OTP_SECRET=your_otp_secret_here

# مفتاح حماية endpoint الأدمن — أنشئه بالأمر:
# openssl rand -hex 16
ADMIN_SETUP_SECRET=your_admin_setup_secret_here

# ─── Firebase Phone Auth (اختياري — fallback لإرسال OTP) ─
# Get from: https://console.firebase.google.com → Project Settings
# Client-side (public)
# NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
# NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# Server-side (private — for verifying Firebase ID tokens)
# Get from: Firebase Console → Project Settings → Service accounts → Generate new private key
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nxxxxxx\n-----END PRIVATE KEY-----\n"

# ─── WhatsApp Cloud API (اختياري — 1000 رسالة مجانية/شهر) ─
# Get from: https://developers.facebook.com → WhatsApp Business
# WHATSAPP_BOT_NUMBER=your_bot_number
# WHATSAPP_PHONE_NUMBER_ID=123456789012345
# WHATSAPP_ACCESS_TOKEN=your_meta_access_token
# WHATSAPP_OTP_TEMPLATE=otp_code

# ─── Push Notifications (موصى به) ─────────────────────────
# Generate with: npx web-push generate-vapid-keys --json
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_EMAIL=mailto:support@maksab.app

# ─── Sentry — مراقبة الأخطاء (موصى به) ────────────────────
# Get from: https://sentry.io → Project Settings → Client Keys (DSN)
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# SENTRY_AUTH_TOKEN=your_sentry_auth_token
# SENTRY_ORG=your_sentry_org
# SENTRY_PROJECT=your_sentry_project

# ─── Payment Integration — Paymob (اختياري) ────────────────
# Get from: https://accept.paymob.com/portal2/en/settings
# Set NEXT_PUBLIC_PAYMOB_ENABLED=true to show Fawry & Card options
# NEXT_PUBLIC_PAYMOB_ENABLED=true
# PAYMOB_API_KEY=your_paymob_api_key
# PAYMOB_CARD_INTEGRATION_ID=your_card_integration_id
# PAYMOB_FAWRY_INTEGRATION_ID=your_fawry_integration_id
# PAYMOB_IFRAME_ID=your_iframe_id
