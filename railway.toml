# Railway deployment configuration for مكسب background workers
# This file configures Railway to build from the Dockerfile in railway/

[build]
dockerfilePath = "railway/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
